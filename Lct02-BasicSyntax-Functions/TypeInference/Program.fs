(*
    Через застосування атрибуту компілятор F# сам виводить тип параметру та тип значення, що повертається.
    Якщо замінити 0 на значення будь-якого іншого типу, то це не змінить сигантуру функції, натомість буде помилка компіляції.
*)

[<EntryPoint>]
let main args =
    printfn "Hello World %d times!" args.Length
    0
